# 🧪 Практическое задание: скачивание последовательностей и редактирование FASTA в терминале

## 🎯 Цели занятия
В этом задании вы научитесь:

- скачивать последовательности COX1 с NCBI;
- переносить файлы в WSL (как будто на удалённый сервер);
- просматривать содержимое файлов через терминал;
- редактировать текстовые файлы (в т.ч. FASTA) с помощью редактора **vim**;
- добавлять новые последовательности в существующий FASTA-файл;
- проверять формат и корректность получившегося файла.

Итогом будет FASTA-файл с **10 последовательностями**:  
7 исходных + 3 новых, скачанных вами.

---

# 📁 Исходные данные
У вас есть файл:

```
studentX_sequences.fasta
```

В нём 7 последовательностей COX1 разных видов.  
Ваша задача — скачать ещё **3 вида**, добавить их в этот файл и получить итоговый FASTA из 10 записей.

---

# 1. 🌐 Скачивание последовательностей с NCBI

Перейдите на сайт:

👉 https://www.ncbi.nlm.nih.gov/nuccore

В поле поиска введите (пример):

```
COX1[Gene] AND Mammalia[Organism] AND 500:900[Sequence Length] AND mitochondrion[Filter]
```

Выберите **новый вид**, которого нет в вашем файле, откройте запись.

Скачайте FASTA:

**Send to → File → FASTA → Create File**

Сохраните как:

```
species1.fasta
```

Повторите для ещё двух видов:

```
species2.fasta
species3.fasta
```

---

# 2. 🗄 Перенос файлов в WSL (как на сервер)

Скачанные файлы лежат в Windows, например:

```
C:\Users\<имя>\Downloads\
```

В WSL они доступны как:

```
/mnt/c/Users/<имя>/Downloads/
```

Проверьте содержимое:

```bash
ls /mnt/c/Users/<имя>/Downloads/
```

Скопируйте свои три FASTA в рабочую директорию WSL:

```bash
cp /mnt/c/Users/<имя>/Downloads/species1.fasta .
cp /mnt/c/Users/<имя>/Downloads/species2.fasta .
cp /mnt/c/Users/<имя>/Downloads/species3.fasta .
```

Теперь у вас в папке:

```
studentX_sequences.fasta
species1.fasta
species2.fasta
species3.fasta
```

---

# 3. 👁 Просмотр файлов в терминале

### Показать весь файл:
```bash
cat species1.fasta
```

### Постраничный просмотр (выход — q):
```bash
less species1.fasta
```

### Посчитать количество символов последовательности (без заголовков):
```bash
grep -v "^>" species1.fasta | wc -c
```

---

# 4. ✏️ Редактирование FASTA-файлов в vim (расширенный разбор)

## 🧠 Как работает vim: три режима

### 1. Normal mode (режим команд)
Режим по умолчанию.  
Используется для навигации, удаления, копирования, выхода в другие режимы.

Выйти в Normal mode откуда угодно:
```
Esc
```

---

### 2. Insert mode (режим ввода текста)
Используется для добавления/редактирования текста.

Войти:
```
i   — начать писать в текущем месте
o   — создать новую строку ниже и писать на ней
O   — создать новую строку выше
```

Внизу появится:
```
-- INSERT --
```

Выйти:
```
Esc
```

---

### 3. Command mode (режим команд редактора)
Открывается для сохранения, выхода, поиска.

Войти (из Normal mode):
```
:
```

Полезные команды:
```
:w      — сохранить
:q      — выйти
:wq     — сохранить и выйти
:q!     — выйти без сохранения
```

---

## 📚 Основные команды навигации

Перейти в конец файла:
```
G
```

Стрелки также работают.

---

# 5. ➕ Добавление новых последовательностей в общий FASTA

## 5.1. Посмотреть содержимое скачанных файлов
```bash
cat species1.fasta
```

Скопируйте блок:
```
>Название_вида
ATGCATGCATGC...
```

---

## 5.2. Открыть основной FASTA-файл
```bash
vim studentX_sequences.fasta
```

Вы в Normal mode.

---

## 5.3. Перейти в конец файла
```
G
```

---

## 5.4. Добавить новую запись
Нажмите:
```
o
```

Вы вошли в insert mode.  
Вставьте последовательность (ПКМ → вставить, или Shift+Insert).

Нажмите:
```
Esc
```

Повторите для species2.fasta и species3.fasta.

---

## 5.5. Сохранить и выйти
```
:wq
```

---

# 6. ✔️ Проверка результата

Проверьте количество заголовков FASTA:

```bash
grep -c "^>" studentX_sequences.fasta
```

Ожидаемый вывод:
```
10
```

---

# 7. 📤 Что нужно сдать

1. Итоговый файл:
```
studentX_sequences.fasta
```
со всеми 10 последовательностями.

2. Краткий отчёт:
   - какие команды использовали для копирования;
   - какие команды vim применяли;
   - какие виды вы добавили.

---

# 8. 🎉 Чему вы научились

- скачивать данные с NCBI;  
- работать с файлами в WSL как на сервере;  
- использовать vim в трёх режимах;  
- корректно редактировать FASTA;  
- проверять формат и содержимое файлов.

Теперь вы готовы к полноценной работе с биоинформатическими пайплайнами на сервере.
